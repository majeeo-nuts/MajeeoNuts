<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>MajeeoNuts - Premium Dry Fruits Online</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 font-sans">

  <header class="bg-green-700 text-white p-6 text-center">
    <h1 class="text-3xl font-bold">MajeeoNuts</h1>
    <p class="mt-2 text-lg">World-class Premium Dry Fruits - Fresh & Healthy</p>
  </header>

  <div class="max-w-7xl mx-auto p-6 grid grid-cols-1 lg:grid-cols-4 gap-6">

    <!-- Products Grid - 3 cols wide -->
    <main class="lg:col-span-3 grid grid-cols-1 sm:grid-cols-2 gap-6">

      <!-- Example Product Card -->
      <article class="bg-white p-6 rounded-lg shadow-md flex flex-col">
        <h2 class="text-xl font-semibold mb-3">Premium Almonds (Badam)</h2>
        <img src="https://images.unsplash.com/photo-1570466181122-7300f4f05543?auto=format&fit=crop&w=400&q=80" alt="Premium Almonds" class="h-48 w-full object-cover rounded mb-4" />
        <p class="text-green-700 font-bold text-lg mb-2">₹1000/kg</p>
        <p class="flex-grow text-gray-700 mb-4">High-quality California almonds, rich in antioxidants and healthy fats. Perfect for snacking and cooking.</p>
        <label for="almond-qty" class="block font-medium mb-1">Select Quantity:</label>
        <select id="almond-qty" name="quantity" class="mb-4 p-2 border rounded">
          <option value="250">250 g</option>
          <option value="500">500 g</option>
          <option value="1000" selected>1 kg</option>
        </select>
        <button class="bg-green-600 text-white py-2 rounded hover:bg-green-700 mb-4 add-to-cart" data-name="Premium Almonds" data-price="1000">Add to Cart</button>
      </article>

      <article class="bg-white p-6 rounded-lg shadow-md flex flex-col">
        <h2 class="text-xl font-semibold mb-3">Premium Cashews (Kaju)</h2>
        <img src="https://images.unsplash.com/photo-1585297132209-2117b49f84d3?auto=format&fit=crop&w=400&q=80" alt="Premium Cashews" class="h-48 w-full object-cover rounded mb-4" />
        <p class="text-green-700 font-bold text-lg mb-2">₹1200/kg</p>
        <p class="flex-grow text-gray-700 mb-4">Fresh, creamy cashews packed with protein and essential minerals. Great for cooking and snacking.</p>
        <label for="cashew-qty" class="block font-medium mb-1">Select Quantity:</label>
        <select id="cashew-qty" name="quantity" class="mb-4 p-2 border rounded">
          <option value="250">250 g</option>
          <option value="500">500 g</option>
          <option value="1000" selected>1 kg</option>
        </select>
        <button class="bg-green-600 text-white py-2 rounded hover:bg-green-700 mb-4 add-to-cart" data-name="Premium Cashews" data-price="1200">Add to Cart</button>
      </article>

    </main>

    <!-- Sidebar: Cart + Sections - 1 col wide -->
    <aside class="bg-white p-6 rounded-lg shadow-md sticky top-6 h-max">

      <!-- Cart Section -->
      <section id="cart-section" class="mb-6">
        <h2 class="text-2xl font-semibold mb-4">Your Cart</h2>
        <div id="cart-items" class="space-y-4 mb-4">
          <p class="text-gray-500">Your cart is empty.</p>
        </div>
        <button id="checkout-btn" class="bg-green-600 text-white py-2 px-4 rounded hover:bg-green-700 w-full" disabled>Checkout</button>
      </section>

      <!-- Home Section -->
      <section id="home-section" class="mb-6">
        <h2 class="text-xl font-semibold mb-2">Home</h2>
        <p>Welcome to MajeeoNuts! Enjoy our premium dry fruits sourced directly from trusted growers.</p>
      </section>

      <!-- About Us Section -->
      <section id="about-section" class="mb-6">
        <h2 class="text-xl font-semibold mb-2">About Us</h2>
        <p>MajeeoNuts is committed to providing fresh and healthy dry fruits with excellent customer service.</p>
      </section>

      <!-- Contact Us Section -->
      <section id="contact-section" class="mb-6">
        <h2 class="text-xl font-semibold mb-2">Contact Us</h2>
        <p>Email: <a href="mailto:maheshgawade7799@gmail.com" class="text-green-700 underline">maheshgawade7799@gmail.com</a></p>
        <p>Phone: +91 9876543210</p>
        <p>Address: Mumbai, India</p>
      </section>

    </aside>
  </div>

  <footer class="bg-green-700 text-white text-center p-4 mt-10">
    <p>© 2025 MajeeoNuts. All rights reserved.</p>
  </footer>

  <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
  <script>
    const cart = [];
    const cartItemsDiv = document.getElementById('cart-items');
    const checkoutBtn = document.getElementById('checkout-btn');

    function renderCart() {
      cartItemsDiv.innerHTML = '';
      if (cart.length === 0) {
        cartItemsDiv.innerHTML = '<p class="text-gray-500">Your cart is empty.</p>';
        checkoutBtn.disabled = true;
        return;
      }
      checkoutBtn.disabled = false;
      cart.forEach((item, index) => {
        const itemDiv = document.createElement('div');
        itemDiv.className = 'flex items-center justify-between border p-2 rounded';

        itemDiv.innerHTML = `
          <div>
            <p class="font-semibold">${item.name}</p>
            <p>₹${item.price} x ${item.quantity} = ₹${item.price * item.quantity}</p>
            <input type="number" min="1" value="${item.quantity}" style="width:60px;" class="quantity-input border rounded p-1 mt-1" data-index="${index}" />
          </div>
          <button class="text-red-600 font-bold px-2" data-index="${index}">Remove</button>
        `;
        cartItemsDiv.appendChild(itemDiv);
      });

      // Add event listeners for quantity inputs and remove buttons
      document.querySelectorAll('.quantity-input').forEach(input => {
        input.addEventListener('change', e => {
          const i = e.target.dataset.index;
          let val = parseInt(e.target.value);
          if (val < 1) val = 1;
          cart[i].quantity = val;
          renderCart();
        });
      });

      document.querySelectorAll('#cart-items button').forEach(button => {
        button.addEventListener('click', e => {
          const i = e.target.dataset.index;
          cart.splice(i, 1);
          renderCart();
        });
      });
    }

    // Add to cart buttons
    document.querySelectorAll('.add-to-cart').forEach(button => {
      button.addEventListener('click', () => {
        const name = button.dataset.name;
        const price = parseInt(button.dataset.price);
        const parent = button.parentElement;
        const select = parent.querySelector('select');
        const quantity = parseInt(select.value);

        const existingIndex = cart.findIndex(i => i.name === name);
        if (existingIndex !== -1) {
          cart[existingIndex].quantity += quantity;
        } else {
          cart.push({ name, price, quantity });
        }
        renderCart();
      });
    });

    // Checkout button
    checkoutBtn.addEventListener('click', () => {
      const options = {
        key: "rzp_live_LIqOpioBHDIhG4", 
        amount: cart.reduce((sum, i) => sum + i.price * i.quantity, 0) * 100, 
        currency: "INR",
        name: "MajeeoNuts",
        description: "Purchase of dry fruits",
        handler: function (response){
          alert("Payment successful! Payment ID: " + response.razorpay_payment_id);
          cart.length = 0;
          renderCart();
        },
        prefill: {
          email: "maheshgawade7799@gmail.com"
        },
        theme: {
          color: "#047857"
        }
      };
      const rzp1 = new Razorpay(options);
      rzp1.open();
    });

    renderCart();
  </script>
</body>
</html>

